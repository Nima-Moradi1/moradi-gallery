// Only publish business claims after they have been explicitly confirmed.
export const business = {
  confirmedBrands: [] as string[],
  consultationUrl: null as string | null,
  warrantyPolicy: null as string | null,
};
